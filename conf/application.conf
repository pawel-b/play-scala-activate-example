# Configuration
application.name=tasks

application.langs=pl

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="E27D^[_<Lpt0vjad]de;3;tx3gpRmG4ByofnahOIo9gbsMWut1w3xg[>9W"

# Database configuration
# ~~~~~ 
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#db.default.driver=org.h2.Driver
#db.default.url="jdbc:h2:mem:tasks"

activate.storage.tasksContext.jdbcDriver=org.h2.Driver
activate.storage.tasksContext.user=""
activate.storage.tasksContext.password=""
activate.storage.tasksContext.url="jdbc:h2:mem:tasks;DB_CLOSE_DELAY=-1"
activate.storage.tasksContext.dialect=h2Dialect
activate.storage.tasksContext.logStatements=true

# Assets configuration
# ~~~~~
"assets.cache./public/stylesheets/bootstrap.min.css"="max-age=3600"
